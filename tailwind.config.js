// const colors = require('tailwindcss/colors');

module.exports = {
  prefix: '',
  mode: 'jit',
  important: false,
  content: ['./src/**/*.{html,ts}'],
  theme: {
  },
  variants: {
  },
  plugins: [
  ],
};
